model:
  model_name_or_path: /home/wwq416/snap/wwq/model/Qwen/Qwen2.5-0.5B-Instruct
  ref_model_name_or_path: null
  trust_remote_code: true
data:
  train_file: /home/wwq416/snap/wwq/math-chain/chaingsm_data/data/final/rl_preprocessed/gsm8k_train_balanced_one_variant_14946/dpo_train.jsonl
  max_samples: null
output:
  output_dir: /home/wwq416/snap/wwq/math-chain/outputs/train/local/dpo/Qwen2.5-0.5B-Instruct/default
training:
  max_length: 3072
  # max_prompt_length and max_completion_length removed in TRL >= 1.5;
  # max_length covers the full sequence (prompt + completion).
  # If separate limits are needed, truncate in preprocessing.
  beta: 0.1
  loss_type: sigmoid
  num_train_epochs: 1
  max_steps: -1
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 16
  learning_rate: 5.0e-6
  lr_scheduler_type: cosine
  warmup_ratio: 0.1
  logging_strategy: steps
  logging_first_step: true
  logging_steps: 10
  disable_tqdm: false
  log_level: info
  save_strategy: "no"
  save_total_limit: 1
  bf16: true
  gradient_checkpointing: true
  report_to: []
  remove_unused_columns: false
lora:
  enabled: false
  r: 16
  alpha: 32
  dropout: 0.05
  target_modules: all-linear
eval:
  enabled: true
  baseline_before_train: true
  data_path: /home/wwq416/snap/wwq/math-chain/chaingsm_data/data/final/gsm8k_test_full/gsm8k_test_all.jsonl
  method: train_json_prompt
  limit: null
  batch_size: 64
  tensor_parallel_size: 1
  gpu_memory_utilization: 0.8
  gpu_memory_utilization_candidates: [0.8, 0.7, 0.6, 0.55, 0.5, 0.45, 0.4, 0.35, 0.3, 0.25]
  dtype: auto
  max_model_len: null
  max_tokens: 2048
  top_k: 1
  top_p: 1.0
  seed: 42
  trust_remote_code: true
  offload_model_before_vllm: true
